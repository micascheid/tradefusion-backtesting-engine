import nomics
from datetime import datetime

import NomicsAPI
from IntervalLimits import interval_limit_dict



if __name__ == '__main__':
    # start = datetime(year=2021, month=11, day=1, hour=0, minute=0, second=0).isoformat()+'Z'
    # end = datetime(year=2021, month=11, day=2, hour=0, minute=0, second=0).isoformat()+'Z'
    #
    # nomics = NomicsAPI.API
    # print(nomics.Candles.get_candles(interval="1h", start=start, end=end, quote="USD", base="BTC"))

    print(1488//1)
